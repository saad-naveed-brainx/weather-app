import 'package:flutter/material.dart';

Class GetStarted entends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        
      )
    );
  }
}